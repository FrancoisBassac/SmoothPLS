% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penalized_functional_regression.R
\name{augment_penalty_matrix}
\alias{augment_penalty_matrix}
\title{Augment a penalty matrix with zero padding for the intercept}
\usage{
augment_penalty_matrix(pen_matrix)
}
\arguments{
\item{pen_matrix}{A square numeric matrix representing the penalty matrix.}
}
\value{
A square matrix with dimensions \code{nrow(pen_matrix) + 1} by
\code{ncol(pen_matrix) + 1}, where the first row and first column are exactly zero.
}
\description{
Expands a penalty matrix by adding a first row and a first column of zeros.
This is required in penalized regression models to ensure that the intercept
term is not penalized, while keeping the matrix dimensions aligned with the
augmented design matrix.
}
\examples{
pen_mat <- matrix(c(2, -1, -1, 2), nrow = 2)
augment_penalty_matrix(pen_mat)
}
\author{
Francois Bassac
}
